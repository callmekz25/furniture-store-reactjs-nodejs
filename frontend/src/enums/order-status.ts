enum OrderStatus {
  PENDING = "pending",
  CONFIRMED = "confirmed",
  SHIPPING = "shipping",
  DELIVERED = "delivered",
  CANCELED = "cancled",
  RETURNED = "retunred",
  FAILED = "failed",
}
export default OrderStatus;
